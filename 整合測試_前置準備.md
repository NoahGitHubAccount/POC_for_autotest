# 整合測試（IT 系列）— 前置準備清單

> 原則：AI 能造的 AI 造（API / autosave 草稿），造不了的標「人工」。狀態：`未備` / `已備` / `進行中`。

## A. 需人工準備（使用者）

| # | 項目 | 用途（代號） | 狀態 | 說明 |
|---|---|---|---|---|
| A1 | 系統管理員帳號 | IT-01 | 已備 | 即現有 `accounts.admin`（config.local.yaml 已填） |
| A2 | 活動管理員帳號 | IT-01 | 未備 | 權限人工先設好，提供帳密 → 填 config `accounts.event_manager` |
| A3 | 一般人員帳號 | IT-01 | 未備 | 同上 → `accounts.staff` |
| A4 | 公民（數位市民）帳號 | IT-09~13「已登入」態、IT-11~13、IT-15 | 未備 | 前台**訪客可瀏覽**（2026-07-11 翻案，見 dom_facts/FrontStage.md）——「尚未登入」態不需帳號即可測；「已登入」態用 `citizen_manual_login.py` 開視窗由使用者實登存 `.auth/citizen` |
| A5 | 工作人員帳號 + 綁定測試活動 | IT-14 | 未備 | 需後台把帳號設為某活動工作人員 |
| A6 | QA 環境帳密（config.test.yaml） | 夜間排程/QA 跑批 | 未備 | 既有待辦，非 IT 系列擋路項 |
| A7 | **admin 密碼更新（疑似季度輪換）** | 所有後台測試 | **未備（2026-07-10 擋路中）** | 舊密碼 `SmartCity20260409` 自 2026-07-09 傍晚起被拒（OCR 已驗證非驗證碼問題）；日期格式暗示季度輪換，請向管理者取得新密碼後更新 `config.local.yaml` |

## B. AI 可自建（測試前置資料）

| # | 項目 | 用途（代號） | 狀態 | 造法 |
|---|---|---|---|---|
| B1 | 空白草稿活動（必填全空） | IT-02 | 進行中 | `lib/ops.create_draft()` 點新增即得 pkid |
| B2 | 各欄位填「上限+1」的活動 | IT-03 | 未備 | create_draft + UI fill（驗證為前端行為，不能走 API） |
| B3 | 測試圖片組（過大/過小/比例不符/>5MB） | IT-04 | 未備 | 本機以 Pillow 生成；`input/1600px_0.png`、`780px_0.png` 已有正常尺寸 |
| B4 | 違規時間組合活動 | IT-05 | 未備 | create_draft + datepicker UI 設值（timedelta 相對日期） |
| B5 | 5 種生命週期狀態活動（啟用前/開放報名前/開放後/開始後/結束後） | IT-08~10 | **可自動造 4/5**（2026-07-11 spike 驗證） | 見下方「IT-08 狀態活動造法」 |
| B6 | 已設定報名欄位的活動 | IT-11~12 | 未備 | UI 設定（報名欄位設定 API 未解碼） |
| B7 | 各憑證狀態報名資料（未報到/已報到/過期/已取消） | IT-12, IT-14 | 未備 | 依賴 A4/A5；過期需時間窗設計 |

## IT-08 狀態活動造法（2026-07-11 spike 驗證）

`disabled` 直接 `ops.create_draft()`；其餘 4 態複製「**2026大港閱冰**冰品嘉年華」（pkid=34155861376110592，真實完整活動含既有 rule；List 的 name 查詢為精確比對，程式內抓全列表過濾「大港閱冰」）再調時間窗：

1. **複製**：`POST /reventmodule/Clone/EVEvent/{源pkid}` → 新 pkid（名稱自動加 `_copy` 尾碼）
2. **調時間**：`PATCH /reventmodule/Entity/EVEvent` 扁平 body `{id, name, startTime, endTime}`（後端接受過去時間）
3. **調開放報名**：從活動 detail `evEventRules[0]` 取 rule id → `PATCH /reventmodule/Entity/EVEventRule` `{id, registrationOpenRule:{"key":"SpecificDateTime"}, registrationOpenFrom}`（⚠ registrationOpenRule 為 VMKeyValue，送字串會 400；pre_open 需設未來，否則複製品既有 regOpenFrom 為過去會落入 registration_open）
4. **啟用**：`PATCH /reventmodule/List/EVEvent/{pkid}/Status` body `{"status":{"key":"2"}}`

stage 判定（後端 `GetFieldEditRules`，時間優先序）：`ended`(now≥EndTime) → `in_progress`(now≥StartTime) → `registration_open`(now≥regOpenFrom) → `pre_open`(isEnabled) → `disabled`。注意 ended/in_progress 不看啟用態。

| stage | startTime | endTime | regOpenFrom |
|---|---|---|---|
| pre_open | +10d | +12d | +5d |
| registration_open | +10d | +12d | -1d |
| in_progress | -1d | +2d | -3d |
| ended | -5d | -1d | -7d |

⚠ 調過時間窗的複製品，既有場次/報到時間與新窗矛盾 → **編輯頁儲存必先被時間驗證擋下**（API 斷言不受影響）。需要「儲存流程能走完」的 UI 測試（如 IT-08 AC8/9 擋值）改用 `make_stage_event(..., keep_source_times=True)`：不動來源時窗、只設 regOpen 過去＋啟用，時間全自洽；但 stage 隨真實時間漂移（來源活動開始後不再是 registration_open），測試須自驗 stage 並 skip。曝光窗已於預設造法同步調整（曝光結束=end+5d）。

## 場次（evEventNodes）時間造態（2026-07-12 IT-10/13 實測）

- **⚠ EVEvent 巢狀 PATCH 的 `evEventNodes` 是整集合置換**：只傳部分場次會**刪掉未傳場次**（7 場剩 1 場實測）。一律全量帶 `{id}`、僅目標場次帶新欄位 → 用 `ops.patch_session_times()`。
- `EVEventNode` 單獨 PATCH / PUT 皆 405。
- 票夾「歷史紀錄」分類看**場次時間**（僅改活動 endTime／改狀態停用都不會進歷史）；場次+活動時間全改過去才進歷史。
- 前台「活動場次」列僅顯示 category=Session 節點（大港閱冰 4 家店家節點不上場次列）。
- 大港閱冰場次 maxAttendees=1（獎品說明1-3）／1500（店家）；`attendees` 欄不隨報名遞增（疑為報到數），已額滿造態不能靠它。
- 憑證過期造態：報名後場次+活動時間全改過去 → 憑證頁 QR 消失（IT-12 AC5）。

## 造出的資料登記

跑測試產生的活動 pkid 一律回填 `STATUS_整合測試.md` 的「測試資料紀錄」表，便於清理。
