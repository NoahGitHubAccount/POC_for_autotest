[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    wbs(id): 測試對應的 WBS 工項編號（例：2-2-2-A）
    module(id): 測試對應的父工項編號（例：2-2-2）
    smoke: 冒煙測試
    regression: 回歸測試
addopts = -ra --strict-markers --import-mode=importlib
