# 整合測試 — 小模型交辦（依循檔）

> 給接手的小模型：**只做本檔列出的工作，照 IT-02 樣板做，不自行發明**。
> 開場先讀：`CLAUDE.md`（行為約束）→ `STATUS_整合測試.md` → `docs/dom_facts/EVEventEdit.md` → 樣板兩檔：
> `specs/integration/IT-02 必填欄位防呆.md` + `tests/integration/test_it02_必填欄位防呆.py`

## 通用規則（違反即停）

1. selector **只准**用 `docs/dom_facts/` 已記載的事實與 `lib/selectors.py`/`lib/ops.py` 既有 API；查不到 → 該 AC 標 `xfail(strict=False)` + 寫入「待確認」，**禁止對 live DOM 盲試、禁止寫一次性 diag 腳本、禁止動 lib/ 與 config**。
2. 測試紅了最多修 **2 輪**；仍紅 → 狀態表標 `阻塞` 並停下回報，不硬修。
3. 每完成一項：更新 `STATUS_整合測試.md`（狀態/首跑結果/測試資料 pkid）。
4. spec 與報告文字**精簡**（使用者要求：避免審閱疲勞）。
5. **每個測試（報告每一列）都必須真實執行完整流程並截圖**——禁止用模組變數快取結果讓後續案例 0 秒通過、截空白圖（IT-02 曾犯，已被使用者抓包）。欄位級驗證寫成一欄一支獨立 test function（參考 IT-02 的 ac2a~ac2e），函式名與 docstring 帶欄位中文名。
6. **斷言不得被必填驗證污染**：空草稿點「儲存變更」必出必填紅框——「紅框>0」「有滾動」「無成功 toast」對字數/格式類規則**不是有效證據**（IT-03 曾因此假陽性）。斷言必須鎖定該規則自身的訊號（如 maxlength 截斷、含該欄位名且非「必填」的紅字）。
7. **截圖必須圖文相符**：點「儲存變更」後畫面會滾回第一個必填錯誤（頁頂），測試結束前一律把目標欄位捲回視窗——xfail 案例也一樣，截圖要看得到「目標欄位＋超量內容＋（無）提示」的證據（IT-03 曾兩度被使用者抓包）。Editor(contenteditable) 驗輸入端攔截必須 `press_sequentially` 實打，`fill()` 會繞過鍵盤事件。紅字過濾關鍵字用**前端 label**（畫面上顯示的欄位名），不要用後端欄位名。
8. **觀測與回報鎖定目標欄位**：案例驗哪個欄位，紅字就只從該欄位容器（`[data-field-id='…'] .errorText`）收集；報告 actual 只寫目標欄位結果並註明「他欄紅字與本案例無關」。全頁紅字列表會誤導審閱者（IT-03 曾被抓包）。
9. **截圖用捲動一律 best-effort**：`scroll_into_view_if_needed` 包 try/except——smooth scroll 動畫中元素 not stable 會逾時，不得因此讓案例 FAIL。**目標欄位在其他 tab 時，儲存後必須先切回目標 tab 再捲動**（儲存會跳回 Tab1，對隱藏元素捲動會靜默失敗、截圖停在 Tab1＝圖文不符——IT-02 AC5 曾五案齊犯）。
11. **不要用 `-k` 子集搭配既有 run_id 重跑**：md_reporter 在 sessionfinish 以「本次 run 的結果」重寫整份報告，子集跑會把批次報告覆蓋成只剩子集案例。要補跑個別案例：用新 run_id，或補跑後**完整重跑該批**收尾。
10. **三段流程（2026-07-11 修訂：匯集≠定版，不得越級凍結）**：
    - 已定版案例（`reports/final/frozen_tests.txt`）自動 deselect，**不要動已凍結案例程式碼**；新案例寫同檔照常跑。
    - 測完 → `python tools/collect_to_review.py <run_id> <IT-xx>` **匯集**到 `reports/review/`（不凍結），回報結果。
    - **定版凍結是使用者的權力**：務必**等使用者明確說某工項 OK** 才可執行 `python tools/finalize_report.py <IT-xx>`。**嚴禁自行 finalize/凍結未經審查的工項**（曾犯）。
    - 發現產品問題：登記 `整合測試_問題單.md` B 區（TSV 照既有列），xfail reason 尾端掛 `[ISS-xxx]`；**不得改 A 主表既有列**。

## 執行環境

```powershell
$env:PYTHONUTF8=1
Set-Location "C:\Workspace\daily-records\專案\FOXCONN\POC_for_autotest"
$env:MD_REPORT_RUN_ID="<YYYYMMDD>_IT0X"   # 每項一個 run_id
.venv\Scripts\pytest.exe "tests/integration/test_it0X_*.py" -v --shot=always
```
- **跑批前一律先刷新 session**（token 伺服器端失效可能早於 1200 分鐘檔案檢查；症狀＝測試全紅且 page.url 停在 `/entry/login`）→ 建臨時 Python 腳本（放 scratchpad）：載入 `lib.config_loader.load_config()`、把 `cfg["captcha"]["mode"]="ocr"` 後呼叫 `lib.auth.perform_interactive_login` + `save_storage_state/save_session_storage`（headless 可）。**不要改 config.local.yaml**。

---

## 工作 1：IT-03 字數上限防呆

規則：欄位輸入超過字數上限，送出時錯誤提示並滾動到該區塊。

- 已盤點上限（後端 `WMAEVEvent.cs` [MaxLength]）：活動名稱 200、活動地點 150、相應網址 2048、Description 250。
  先 grep 該檔補完整清單，只取**前端 EVEventEdit 有對應輸入框**的欄位（對照 `lib/ops.py` 的 F_* 常數：F_NAME/F_LOCATION/F_EXT_TEXT/F_EXT_URL…）。
- **前後端上限不一致事實（2026-07-09 截圖佐證）**：前端「活動名稱」輸入框有字數 counter 顯示 `0 / 100`（前端上限 100 ≠ 後端 MaxLength 200）。**以前端 counter 值為測試上限**，各欄位實際上限以頁面 counter 讀值為準（`<目前字數> / <上限>` 文字就在輸入框右下），並把前後端差異記入 spec「實作差異註記」。
- 步驟（仿 IT-02）：`ops.create_draft` → 對每欄 `fill("x"*(上限+1))` + Tab → 點「儲存變更」。
- **注意兩種可能結果**（先跑第一輪觀察，再定斷言並記入 spec「實作差異註記」）：
  a) 前端 input 有 `maxlength` 屬性 → 輸入被截斷（value 長度==上限）＝防呆成立
  b) 可輸入超量 → 送出後 `.errorText` 出現該欄位警示（記錄實際文字）
- 產出：`specs/integration/IT-03 字數上限防呆.md`（照 IT-02 spec 格式）+ `tests/integration/test_it03_字數上限防呆.py`（parametrize 欄位×上限）。

## 工作 2：IT-07 Autosave 草稿保留

規則：欄位填寫完成需 autosave 成草稿，重開後資料保留（app 於欄位 blur 時自動 PATCH，已實測可行）。

- 步驟：`ops.create_draft` 取 pkid → `ops.fill_text_field(page, ops.F_NAME, "IT07_autosave_<時間戳>")`（fill+Tab 觸發 blur autosave）→ `page.wait_for_timeout(2000)` → **不點儲存** → `ops.open_edit` 重開 → `ops.read_field` 驗名稱等於所填值。
- 反例 AC（選配）：重開後其他未填欄位仍空。
- 產出：`specs/integration/IT-07 Autosave草稿保留.md` + `tests/integration/test_it07_autosave草稿保留.py`。

## 工作 3：收尾（每項做完都要）

1. 跑 pytest 產報告（`--shot=always`），把「狀態/首跑結果/報告路徑」回填 `STATUS_整合測試.md`，草稿 pkid 記入「測試資料紀錄」。
2. 全部做完後提醒使用者：`.\scripts\sync_to_company.ps1` + aiautotest git push。

## 禁區（留給 Fable 5，不要碰）

- IT-04（圖片裁切斷言設計）、IT-05（時間規則，與 2-3-13 重疊界定）、IT-06（前景下載 blocker）、IT-08~16（造資料/公民端/SSO/工作人員）
- `tools/dom_probe.py` 新 DOM 探勘、`docs/dom_facts/` 新事實回填（可讀不可寫）
- 修改 `lib/`、`conftest.py`、`pytest.ini`、config
